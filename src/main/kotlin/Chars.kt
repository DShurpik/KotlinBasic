fun main() {
    val a: Char = 'a'
    println(a)

    val digit: Char =
        '1' // Если значение символьной переменной – цифра, её можно явно преобразовать в Int с помощью функции digitToInt().
    println(digit.digitToInt())
}